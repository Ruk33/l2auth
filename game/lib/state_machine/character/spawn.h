#ifndef LIB_STATE_MACHINE_CHARACTER_SPAWN_H
#define LIB_STATE_MACHINE_CHARACTER_SPAWN_H

#include <headers.h>
#include <request.h>
#include <character.h>

void state_machine_character_spawn(request_t *request, character_t *character);

#endif
