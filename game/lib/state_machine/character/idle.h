#ifndef LIB_STATE_MACHINE_CHARACTER_IDLE_H
#define LIB_STATE_MACHINE_CHARACTER_IDLE_H

#include <headers.h>
#include <request.h>

void state_machine_character_idle(request_t *request, character_t *character);

#endif
