#ifndef LIB_STATE_MACHINE_CHARACTER_SELECTION_H
#define LIB_STATE_MACHINE_CHARACTER_SELECTION_H

#include <headers.h>
#include <request.h>

void state_machine_character_selection(request_t *request);

#endif
